- hosts: localhost
  tasks:
  - name: "Install all the software"
    become: true
    package:
      name: "{{ packages }}"
      state: present
    vars:
      packages:
      - ripgrep
      - xclip
      - jq
      - tldr
